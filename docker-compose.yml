version: "3.8"

services:
  postgres:
    image: postgres:15
    container_name: cfd_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cfd
    ports:
      - "5432:5432"